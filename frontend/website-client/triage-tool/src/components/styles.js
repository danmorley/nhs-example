import styled from "styled-components"

const colors = {
  teal: "#138585"
}

const TriageToolContainer = styled.div`
  min-width: 320px;
  min-height: 480px;
  background-color: ${colors.teal}
`

export { TriageToolContainer }
